{
  "name": "@fusion/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Fusion — standalone ML/DS IDE (Electron host). Reuses @fusion/core + the Svelte cockpit; grows into a full editor shell (Monaco, file tree, terminal).",
  "main": "dist/main.js",
  "scripts": {
    "build:host": "tsc -p .",
    "build": "tsc -p . && npm --prefix ../../webview-ui run build",
    "app": "electron .",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.5"
  }
}
